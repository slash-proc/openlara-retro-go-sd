**/build/
*.bin
*.elf
# Host SDL binaries (make host)
OpenLara_host
*_host
host_saves/
*.map
*.o
*.d
*.su
*.lst
.DS_Store
__pycache__/
# Fetched by ./scripts/fetch_openlara.sh (do not commit the nested clone)
third_party/OpenLara/
# Large game assets (PKD) — keep on SD / local only
sd_assets/
